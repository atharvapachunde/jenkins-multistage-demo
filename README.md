# jenkins-multistage-demo
Multi-stage build pipeline with branch merging ( app: inventory-api )

Branches : dev, feature-api, feature-ui

Jenkins pipeline with build, test and deploy stages.

Merge feature-api --> dev --> trigger build
